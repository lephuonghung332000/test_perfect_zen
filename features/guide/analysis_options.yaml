include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/server/*.g.dart
    - test/_data/**
    - build/**
    - lib/generated/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    public_member_api_docs: false
